import React from "react";

import Note from "@/components/Note";

const Home = () => {
    return (
        <div>
            <h1 style={{ textAlign: "center", margin: "40px", color: "#b4b4c9" }}>
                Default Home screen not yet decided, access other routes through NAVBAR and SIDEBAR as well as DIRECT
                LINKS(i.e for pages not in either)
            </h1>
            <Note />
        </div>
    );
};

export default Home;
